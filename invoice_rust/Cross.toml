# Cross 配置文件 - 适配 OrbStack

[build]
# 使用预构建的 Docker 镜像
[target.x86_64-pc-windows-gnu]
image = "ghcr.io/cross-rs/x86_64-pc-windows-gnu:latest"

# 禁用默认的工具链检查
[build.env]
passthrough = [
    "CARGO_HOME",
    "RUSTUP_HOME",
]
